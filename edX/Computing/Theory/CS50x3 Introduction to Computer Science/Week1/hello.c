/*In order to compile it, you should type in the shell
  cc hello.c, to execute it, you must type ./a.out in 
  a UNIX system, otherwise, consult thw web for help.
  Another way to compile it and execute it is the common
  GCC form*/
#include <stdio.h>
int main(void)
{
	printf("Hello, world\n");
	return(0);
}
